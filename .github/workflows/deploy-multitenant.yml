name: Deploy Frontend to S3 and CloudFront (Multi-Tenant)

on:
  push:
    branches:
      - multi-tenant
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-multitenant.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build Project
        working-directory: ./frontend
        run: VITE_API_URL=https://pos-api.intratechno.com/api/v1 npm run build

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.MULTITENANT_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.MULTITENANT_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync to S3
        run: |
          aws s3 sync ./frontend/dist s3://${{ secrets.MULTITENANT_S3_BUCKET_NAME }} --delete

      - name: Invalidate CloudFront Cache
        run: |
          aws cloudfront create-invalidation --distribution-id ${{ secrets.MULTITENANT_CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"
